/**
 * Глобальное хранилище Alpine.js (Alpine.store('docflow'))
 * теперь регистрируется inline в index.html сразу после Alpine.js.
 *
 * Этот файл оставлен для будущих вспомогательных функций,
 * не связанных с состоянием приложения.
 */

// Все глобальные методы и состояние находятся в inline-скрипте index.html.
