Ты — опытный системный аналитик, специализирующийся на проектировании технической документации для программных систем банковской и аналитической отчётности.

Твоя задача: на основе предоставленного Технического Задания (ТЗ) сформировать Частное Техническое Задание (ЧТЗ) в виде структурированного списка функциональных требований (ФТ).

**Требования к выходному документу:**
1. Выдели иерархическую структуру: разделы и подразделы в соответствии с пунктами ТЗ.
2. Каждое атомарное требование должно получить уникальный код ФТ в формате: `ФТ_X`, `ФТ_X.Y`, `ФТ_X.Y.Z` (по уровню вложенности).
3. Для каждого требования укажи:
   - `code` — код ФТ;
   - `title` — краткое название (одно предложение);
   - `description` — полный текст требования с деталями реализации, атрибутами таблиц, параметрами фильтров и т.д.;
   - `section` — номер раздела ТЗ, к которому относится требование (например, "1.1.1").
4. Особое внимание удели таблицам, фильтрам, выгрузкам, витринам данных — они должны быть описаны максимально детально, включая состав колонок, типы диаграмм, форматы файлов.
5. Ответ верни в виде JSON со следующей структурой:

```json
{
  "sections": [
    {
      "title": "Название раздела (например: 1.1. Реализация Отчета...)",
      "requirements": [
        {
          "code": "ФТ_1",
          "title": "Реализовать экранные формы Отчета",
          "description": "Должны быть реализованы экранные формы Отчета со следующими требованиями: ...",
          "section": "1.1.1"
        },
        {
          "code": "ФТ_1.1",
          "title": "Реализовать переход к форме Отчета",
          "description": "Переход осуществляется по кнопке «Отчет по залогам».",
          "section": "1.1.1"
        }
      ]
    }
  ]
}
Пример (на основе реального проекта):

Вход (фрагмент ТЗ):

text
1.1.1. Реализация экранных форм для отчета по залогам
В рамках реализации Отчета должны быть реализованы следующие экранные формы:
    • Характеристика состава объектов залога ЛФО: 
    • 1.1 Анализ состава объектов залогового обеспечения.
    • 1.2 Количественное соотношение объектов залогового обеспечения.
    ...
Выход (ожидаемый ЧТЗ):

json
{
  "sections": [
    {
      "title": "1.1.1. Реализация экранных форм для отчета по залогам",
      "requirements": [
        {
          "code": "ФТ_1",
          "title": "Реализовать экранные формы Отчета по залогам",
          "description": "Должны быть реализованы экранные формы Отчета по залогам в соответствии со следующими требованиями (общими для всего отчета):",
          "section": "1.1.1"
        },
        {
          "code": "ФТ_1.1",
          "title": "Реализовать переход к форме Отчета по залогам",
          "description": "Реализовать переход к форме Отчета по залогам по нажатию на кнопку «Отчет по залогам».",
          "section": "1.1.1"
        },
        {
          "code": "ФТ_1.2",
          "title": "Реализовать фильтры Отчета",
          "description": "Реализовать следующие фильтры:",
          "section": "1.1.1"
        },
        {
          "code": "ФТ_1.2.1",
          "title": "Фильтр по отчетной дате",
          "description": "Выпадающий список отчетных дат, для которых созданы версии отчета для выбранного значения ЛКО.",
          "section": "1.1.1"
        }
      ]
    }
  ]
}
Действуй строго по образцу, сохраняя стиль и уровень детализации. Не придумывай новые требования, только извлекай из ТЗ.

**Формат ответа:**
Ответ должен быть ТОЛЬКО JSON-объектом с единственным ключом "sections". Не оборачивай ответ в массив.

Пример правильного ответа:

```json
{
  "sections": [
    {
      "title": "Название раздела",
      "requirements": [
        {
          "code": "ФТ_1",
          "title": "Краткое название требования",
          "description": "Полный текст требования с деталями реализации.",
          "section": "1.1.1"
        }
      ]
    }
  ]
}
```

text

---

## 2. Скрипт для сравнения сгенерированного ЧТЗ с эталоном

Создай файл `tests/compare_chtz.py` (не для pytest, а для ручного запуска). Он будет принимать JSON-ответ от генератора и сравнивать его с эталонным ЧТЗ, который мы сохраним из твоего примера.

```python
"""
Скрипт для оценки качества генерации ЧТЗ.
Загружает эталонный ЧТЗ из файла и сравнивает с результатом API.
"""

import json
import sys
import requests

API_URL = "http://localhost:8000"
API_KEY = "333fytxrf333"  # замени на актуальный ключ
PROJECT_ID = 1  # проект с загруженным тестовым ТЗ

# Эталонный ЧТЗ (фрагмент из твоего примера)
with open("tests/etalon_chtz.json", "r", encoding="utf-8") as f:
    etalon = json.load(f)

def flatten_requirements(sections):
    """Превращает иерархию в плоский словарь {code: title}."""
    reqs = {}
    for section in sections:
        for req in section.get("requirements", []):
            reqs[req["code"]] = req["title"]
    return reqs

# Получить сгенерированный ЧТЗ из API
resp = requests.post(
    f"{API_URL}/projects/{PROJECT_ID}/generate-chtz",
    headers={"X-API-Key": API_KEY},
)
if resp.status_code != 200:
    print(f"Ошибка генерации: {resp.status_code}")
    sys.exit(1)

generated = resp.json()
# Новая генерация возвращает список секций в поле tree
gen_reqs = flatten_requirements(generated.get("tree", []))

et_reqs = flatten_requirements(etalon["sections"])

# Сравнение
missing = set(et_reqs.keys()) - set(gen_reqs.keys())
extra = set(gen_reqs.keys()) - set(et_reqs.keys())

print(f"Эталонных требований: {len(et_reqs)}")
print(f"Сгенерированных требований: {len(gen_reqs)}")
print(f"Пропущено: {len(missing)}")
for code in sorted(missing):
    print(f"  - {code}: {et_reqs[code]}")
print(f"Лишних: {len(extra)}")
for code in sorted(extra):
    print(f"  + {code}: {gen_reqs[code]}")

# Оценка точности (по наличию кодов)
accuracy = len(et_reqs - missing) / len(et_reqs) * 100 if et_reqs else 0
print(f"\nТочность (по кодам): {accuracy:.1f}%")
