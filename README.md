# 🧪 QA Test Case Generator

**Микро-SaaS приложение (MVP) для автоматической генерации тест-кейсов по описанию задачи с помощью LLM (OpenAI, Mistral AI, Groq, Ollama и др.).**

Позволяет QA-инженерам настраивать структуру тест-кейса (шаблон с динамическими атрибутами) и получать готовые кейсы, строго следующие заданной форме.
**Новая возможность:** загружайте скриншоты интерфейсов, получайте их текстовое описание через мультимодальную LLM и используйте эти описания в тест-кейсах через плейсхолдеры `{{Имя страницы}}`.

**Новинка:** интерактивный чек-лист проверок — перед генерацией кейсов система формирует структурированный перечень всех возможных проверок (позитивные/негативные, функциональные/нефункциональные), а вы выбираете, по каким пунктам генерировать тест-кейсы.

---

## ✨ Возможности

- **Конструктор шаблона** – добавляйте/удаляйте любые поля тест-кейса (название, предусловия, шаги, ожидаемый результат, окружение и т.д.)
- **Генерация через LLM** – поддерживается несколько провайдеров (OpenAI, Mistral, Groq, OpenRouter, Ollama)
- **Библиотека страниц** – загрузка скриншотов, автоматическое описание страницы через мультимодальную модель (Mistral Pixtral / LLaVA) и сохранение в SQLite
- **Плейсхолдеры в описании задачи** – пишите `{{Имя страницы}}` в поле задачи, и система подставит сохранённое описание
- **Строгая валидация ответа** – динамическая Pydantic-схема гарантирует, что модель вернёт данные только с указанными атрибутами
- **Копирование в буфер** – результат отображается в структурированном виде и может быть скопирован как JSON
- **Лёгкий фронтенд** – один HTML-файл с Tailwind CSS и ванильным JS (без фреймворков)
- **Готовность к деплою** – FastAPI отдаёт и бэкенд, и статику
- **Интерактивный чек-лист проверок** – автоматическая генерация структурированного списка проверок (позитивные/негативные × функциональные/нефункциональные) с возможностью выбора нужных пунктов перед генерацией тест-кейсов
- **Выборочная генерация** – каждый пункт чек-листа превращается в 1+ детализированных тест-кейсов с автоматическим проставлением типа и категории проверки

---

## 🖼️ Интерфейс

1. Переключайтесь между вкладками **«Генератор»**, **«Библиотека страниц»** и **«Чек-лист»**
2. В библиотеке загружайте скриншоты, давайте им уникальные имена – система опишет страницу через LLM
3. В генераторе настройте поля шаблона, вставьте описание задачи (можно с `{{Имя страницы}}`), нажмите «Сгенерировать тест-кейсы» и получите структурированный результат
4. На вкладке **«Чек-лист»** введите описание функционала, нажмите «Сформировать чек-лист» — система создаст структурированный перечень проверок, разбитый на позитивные/негативные и функциональные/нефункциональные. Отметьте нужные пункты (или используйте групповые чек-боксы) и нажмите «Сгенерировать тест-кейсы по выбранным проверкам»

---

## 🧱 Архитектура
qa-case-generator/  
├── backend/  
│ ├── init.py  
│ ├── main.py # FastAPI приложение (эндпоинты, статика, подстановка плейсхолдеров)  
│ ├── database.py # SQLite для библиотеки страниц  
│ ├── pages.py # Роутер загрузки/списка/удаления скриншотов  
│ └── static/  
│ ├── index.html # Фронтенд (HTML + Tailwind CDN + встроенный JS)  
│ └── uploads/ # Сохранённые скриншоты  
├── requirements.txt  
├── .env # API-ключи и конфигурация (не коммитить!)  
└── README.md  

## 🛠 Технологии
- **Бэкенд**	Python 3.10+, FastAPI, Pydantic
- **Фронтенд**	HTML5, Tailwind CSS (CDN), Vanilla JS
- **LLM**	OpenAI API / Mistral / Groq / OpenRouter / Ollama
- **Vision LLM**	Mistral Pixtral (или LLaVA) через Mistral API
- **База данных**	SQLite (встроенная, файлы pages.db)
- **Валидация**	Динамические модели Pydantic с алиасами

---

## ⚙️ Быстрый старт

### 1. Клонируйте репозиторий
```bash
git clone https://github.com/Ann-123/Case_Generator.git
cd Case_Generator
```
### 2. Настройте переменные окружения
Создайте файл .env в корне проекта. Пример для Mistral AI:
```
LLM_PROVIDER=mistral
MISTRAL_API_KEY=mistral_ваш_ключ
LLM_MODEL=open-mistral-nemo
VISION_MODEL=pixtral-12b-2409   # модель для распознавания скриншотов
```
### 3. Установите зависимости
```
python -m venv venv
source venv/bin/activate      # Windows: venv\Scripts\activate
pip install -r requirements.txt
```
### 4. Запустите сервер
```
uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000
```
### 5. Откройте приложение
Перейдите на http://localhost:8000

---

## 📡 API Endpoints

### POST /generate – генерация тест-кейсов
Принимает:
```
{
  "task_text": "Описание задачи... Можно с {{Страница логина}}",
  "fields": ["Название", "Предусловия", "Шаги", "Ожидаемый результат"],
  "checklist_items": []  // опционально: пункты чек-листа для выборочной генерации
}
```

### POST /generate-checklist – генерация чек-листа проверок
Принимает:
```
{
  "task_text": "Описание функционала для составления чек-листа"
}
```
Возвращает структурированный чек-лист:
```
{
  "checklist": {
    "positive": {
      "functional": [{"id": "p-f-1", "text": "..."}, ...],
      "non_functional": [...]
    },
    "negative": {
      "functional": [{"id": "n-f-1", "text": "..."}, ...],
      "non_functional": [...]
    }
  }
}
```

### POST /pages/upload – загрузка скриншота
Отправьте ```multipart/form-data``` с полями ```file``` (изображение) и ```name``` (уникальное имя).
Модель вернёт описание страницы, которое сохранится в SQLite.

### GET /pages/list – список сохранённых страниц
### DELETE /pages/{name} – удалить страницу
